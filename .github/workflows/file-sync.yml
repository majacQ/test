name: File Sync

on:
  push:
    branches:
      - master
  schedule:
    - cron: 0 0 * * *

jobs:
  file_sync:
    runs-on: ubuntu-latest
    steps:
      - name: Fetching Local Repository
        uses: actions/checkout@master
      - name: File Sync
        uses: kbrashears5/github-action-file-sync@master
        with:
          REPOSITORIES: |
            kbrashears5/test@test1
          FILES: |
            ./newdestination/test.txt=./newdestination4/test.txt
            ./newdestination/test.txt = ./newdestination5/test.txt
            ./.github/workflows=newFolder
          TOKEN: ${{ secrets.ACTIONS }}
          PULL_REQUEST: false